const Events = () => {
    return (
        <div style={{ padding: '2rem' }}>
            <h1 className="stranger-title" style={{ fontSize: '3rem' }}>Events</h1>
            <p>Upcoming events from IEEE Procomm.</p>
        </div>
    );
};

export default Events;
